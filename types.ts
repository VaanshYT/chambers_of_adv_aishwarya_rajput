export interface PracticeArea {
  id: string;
  title: string;
  description: string;
  details: string[];
}

export interface Feature {
  id: string;
  text: string;
}

export interface NavItem {
  label: string;
  href: string;
}
